# 02 PRD 与验收（v0.1）

> 来源：`VibeCoding开发全流程SOP_详细可填写版.xlsx` 的 `02_PRD与验收`。
> 这是基于已确认需求形成的第一版范围。用户已回复“继续”，后续按本文默认规则推进；新增要求进入变更管理。

## 产品目标

建设一个对外开放注册、按公司隔离数据的团队项目进度平台。团队母账号注册并创建团队后，团队成员使用用户名和密码进入团队，查看和维护项目。未加入团队的外部用户不能阅读团队数据。每家公司可以配置自己的项目状态。系统不向 OpenAI 或其他外部 AI 服务发送项目资料。

## 角色与权限

| 角色 | 权限范围 |
| --- | --- |
| 团队母账号/公司管理员 | 注册公司与团队、创建成员、管理成员、配置项目状态、管理本公司项目 |
| 团队成员 | 登录团队、按当前团队分配的自定义角色查看或更新项目进度 |
| 未加入团队的外部用户 | 无法查看团队项目、资料和查询结果 |

## 核心用户故事

### US-01 注册团队母账号

作为新公司负责人，我希望注册团队母账号并创建公司团队，从而建立属于公司的独立工作空间。

### US-02 进入团队

作为团队用户，我希望使用用户名和密码登录并进入所属团队，从而查看团队项目。

### US-03 隔离公司数据

作为公司管理员，我希望本公司的项目和资料只对本公司团队成员可见，从而避免公司之间的数据互相影响。

### US-04 管理团队成员

作为团队母账号，我希望创建、停用和管理团队成员账号，从而控制谁可以进入团队。

### US-05 配置公司项目状态

作为公司管理员，我希望自定义本公司的项目状态，从而适配不同公司的项目管理方式。

### US-06 管理项目进度

作为团队成员，我希望查看和更新项目阶段、进度、负责人、阻塞项、计划日期和资料来源，从而保持项目事实一致。

### US-07 查询项目事实

作为团队成员，我希望按项目或问题查询项目事实，从而快速了解项目状态；系统只能返回本团队已有资料支持的结论。

## 正常、异常与边界场景

| 场景 | 预期行为 |
| --- | --- |
| 注册时用户名已存在 | 拒绝注册并提示重新填写，不泄露其他团队信息 |
| 密码不符合安全规则 | 拒绝提交并说明规则，不保存明文密码 |
| 登录密码错误 | 拒绝登录，记录安全事件并执行限流/锁定策略 |
| 用户访问其他公司项目 ID | 服务端返回无权限或不存在，不能泄露项目是否存在 |
| 成员被停用后继续使用旧会话 | 会话失效，不能继续访问团队数据 |
| 团队没有项目 | 展示空状态，不生成虚构的项目结论 |
| 查询没有匹配资料 | 明确说明没有找到已验证数据 |
| 自定义状态被项目使用 | 不允许直接删除；要求迁移项目或停用状态 |
| 项目必填字段缺失 | 服务端拒绝保存，并返回可修复的字段提示 |
| 两个成员同时更新项目 | 保留明确的更新时间/版本信息，避免静默覆盖 |

## 初版验收标准

### AC-01 团队注册与登录

- Given 用户未登录，When 注册团队母账号并提交合法用户名和密码，Then 系统创建公司/团队空间并进入团队首页。
- Given 用户已注册，When 使用正确账号密码登录，Then 系统只进入其所属团队。
- Given 用户输入错误密码，Then 登录失败，不返回团队资料，并触发安全限制记录。

密码规则：新注册的团队母账号和新创建的成员账号，密码长度必须为 6-20 个字符。

### AC-02 公司隔离

- Given 公司 A 和公司 B 都有项目，When 公司 A 用户查询或访问项目，Then 只能看到公司 A 的数据。
- Given 公司 A 用户伪造公司 B 的项目 ID，Then 服务端拒绝访问，响应不能透露公司 B 项目是否存在。

### AC-03 团队成员

- Given 团队母账号创建成员，When 成员使用账号登录，Then 成员可以进入该团队。
- Given 团队母账号停用成员，When 被停用成员再次访问，Then 访问被拒绝并且旧会话失效。

### AC-04 自定义状态

- Given 公司管理员创建状态“方案评审中”，When 将其用于本公司项目，Then 项目可以保存并展示该状态。
- Given 状态仍被项目使用，When 管理员尝试删除该状态，Then 系统阻止删除并提示先处理关联项目。

### AC-05 项目查询

- Given 团队内已有项目资料，When 团队成员查询项目，Then 返回项目状态、阶段、负责人、风险、阻塞项和来源。
- Given 没有已验证资料，Then 系统明确返回资料不足，不使用外部 AI 补全答案。

## 暂不纳入第一版

- 真实文件上传、文档解析和自动资料抽取。
- OpenAI 或其他外部 AI 调用。
- 未加入团队的外部只读访问。
- 支付、订单执行、库存扣减等业务动作。

## 仍需确认

1. 团队母账号是否可以创建多个管理员。
2. 是否增加项目级授权，以补充当前已实现的团队级自定义角色。
3. 用户名格式和找回密码；账号锁定策略已实现，密码长度已确认采用 6-20 个字符。
4. 项目状态排序等增强规则；名称、颜色、启停、归档和引用保护已实现。
5. 项目永久删除策略；项目归档和团队快照导出已实现，永久删除暂不开放。
