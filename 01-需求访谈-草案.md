# 01 需求访谈（草案）

> 来源：`VibeCoding开发全流程SOP_详细可填写版.xlsx` 的 `01_需求访谈`。
> 本草案基于现有项目代码、README 和用户已确认的 00 阶段边界，尚未替代正式业务访谈确认。

## 01-01 项目背景

当前项目是一个电商项目进度助手原型，用项目状态、当前阶段、负责人、阻塞项、更新时间和资料来源回答项目问题。现有版本能够展示 3 个硬编码项目，但真实使用前存在以下问题：

- 项目数据无法持久化和维护。
- 资料来源只是文本标签，没有可追溯的真实文件或记录。
- 用户不限定为内部团队，当前没有登录、角色和项目可见范围控制。
- 当前代码存在调用 OpenAI 的路径，与“不允许发送项目资料”的边界冲突。

## 01-02 目标用户（已确认）

| 角色 | 可能目标 | 待确认事项 |
| --- | --- | --- |
| 团队母账号/公司管理员 | 注册公司空间、开启团队模式、创建成员账号、管理权限和团队状态 | 是否支持多个管理员待后续确认 |
| 团队成员 | 使用用户名和密码进入团队，查看或更新被授权的项目 | 具体编辑范围进入 PRD 验收 |
| 未加入团队的外部用户 | 不允许查看团队项目或资料 | 不提供外部只读访问 |

## 01-03 初步项目目标

1. 将当前硬编码演示项目升级为可持久化、可维护的数据驱动多租户系统。
2. 保证不同公司之间的数据、项目、成员和资料互不影响。
3. 让授权用户能够查看项目进度、阶段、负责人、风险、阻塞项和来源。
4. 在不调用 OpenAI 或其他外部 AI 服务的前提下，提供可解释、可追溯的本地查询结果。
5. 通过公司、团队、项目和角色权限控制真实资料的可见范围。
6. 以安全、项目架构和速度作为第一版非功能优先级。
7. 支持团队母账号注册、团队成员账号和用户名/密码登录。
8. 支持每家公司自定义自己的项目状态。

## 01-04 第一版建议范围（待确认）

### 建议纳入

- 公司/租户、团队、成员、角色和权限的持久化。
- 项目、阶段、状态、负责人、计划日期、进度、阻塞项和来源记录的持久化。
- 项目新增、编辑、查看和状态更新，并强制执行租户范围校验。
- 项目组合总览、风险筛选和项目详情。
- 关键词/结构化条件查询，以及基于本地规则的自然语言问题解析。
- 资料来源记录与权限校验；真实文件上传列为后续阶段。
- 登录、会话、成员邀请、角色权限和跨租户访问防护。
- 团队母账号注册、团队成员账号管理和用户名/密码登录。
- 公司级项目状态配置，并防止删除仍被项目使用的状态。
- 服务端输入校验、权限校验、错误处理、审计记录和性能基线。

### 建议暂不纳入

- OpenAI 或其他外部 AI 服务调用。
- 第一版的真实资料文件上传（用户已确认“后续上传”）。
- 自动读取未授权的网盘、邮箱、聊天记录或第三方系统。
- 支付执行、订单执行、库存扣减等高风险业务动作。
- 未经确认的公开分享、自动通知和自动发布。

## 已确认的非功能优先级

1. 安全：优先保证登录、权限、租户隔离、敏感数据保护和审计。
2. 项目架构：优先建立可扩展的多租户数据模型和清晰的模块边界。
3. 速度：优先保证页面响应、查询响应和后续迭代效率。

## 当前待确认问题

1. 团队母账号是否可以创建多个管理员。
2. 第一版核心操作顺序暂按“注册团队母账号 → 进入团队 → 管理成员和项目”进入 PRD；具体项目操作在 02 阶段固化。
3. 真实资料中的敏感字段有哪些，哪些团队角色可以查看。
4. “项目状态”已确认由每家公司自定义；“项目阶段”作为独立字段处理，避免和状态混在一起。

## 访谈状态

- 00 阶段边界：已确认。
- 01 背景、目标、用户范围、登录方向和状态自定义方向：已确认。
- 多管理员规则、成员编辑权限、敏感字段细分和资料上传规则：在 02 PRD 和后续安全设计中继续确认。
