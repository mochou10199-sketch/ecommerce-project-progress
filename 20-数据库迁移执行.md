# 20 数据库迁移执行

## 20-01 目标架构

按照项目约束“安全、项目架构、速度”，并根据“本次不需要已有数据”的决定，目标架构确定为全新 Supabase PostgreSQL：

```text
浏览器 → Node/Vinext 主机 → Drizzle ORM → Supabase PostgreSQL
                                      ↑
                         DATABASE_URL（服务端密钥）
```

- PostgreSQL 是新版本活动数据源，继续使用当前服务端用户名/密码会话和团队权限模型。
- 每个团队继续使用 `team_id` 做服务端隔离；没有新增外部分享、外部 AI 或浏览器端数据源。
- Supabase 只提供 PostgreSQL 托管和备份能力；应用权限仍由本项目服务端控制。
- 原 Cloudflare Worker/D1/Sites 版本暂不删除，仅作为旧版本参考和可选回滚资产；本次不导出、不导入已有 D1 数据。

## 20-02 已完成的本地工作

1. 拆分 SQLite/D1 与 PostgreSQL Drizzle schema，保持表名和字段名兼容。
2. `DATABASE_URL` 存在时，Node/Vinext 使用 PostgreSQL；无该变量时保留旧 D1 兼容路径。
3. 生成 `drizzle-postgres/0000_fixed_killmonger.sql`，包含 10 张核心表、索引和 `team_roles`。
4. 导入脚本默认只允许本机目标，拒绝非空库，使用单事务写入，并为历史团队补齐 `owner`/`member` 系统角色。
5. Node 运行时已补齐安全响应头、跨站写入拦截和 256 KiB API 请求体门禁。
6. 本地 D1 快照已导入独立 PostgreSQL 演练库：12 个团队、16 个用户、6 个项目；角色表最终有 24 条记录。

## 20-03 可复现命令

```bash
# 生成/检查结构
npm run db:postgres:generate
npm run db:postgres:verify

# 从 SQLite/D1 导出导入本机 PostgreSQL；目标库必须为空
npm run db:postgres:import -- --sqlite /受控目录/d1-export.sqlite

# Node/Vinext 运行时验收
DATABASE_URL='postgresql://...' npm run postgres:acceptance
```

导入工具还支持 `--dump /受控目录/d1-export.sql`。脚本不会覆盖已有数据；远端目标必须额外显式添加 `--allow-remote`。生产连接串不得提交到仓库、前端变量或日志。

## 20-04 线上切换门禁

本次采用全新库上线，以下顺序不能跳过：

1. 创建 Supabase PostgreSQL 项目，确认区域、备份策略、数据库用户和连接池方式；只把服务端连接串放入部署平台密钥。**已完成：项目 `glybymearklqjgvsguax` 已健康运行。**
2. 在 Supabase 全新目标库执行 `drizzle-postgres/0000_fixed_killmonger.sql`，再只读校验 10 张表、关键字段、索引和空库状态；本次不执行 D1 数据导入。**已完成：10 张表已创建并校验。**
3. 设置新主机的 `DATABASE_URL`，执行 `npm ci`、`npm run build`、`npm run postgres:acceptance`，再用新注册团队、成员权限、项目编辑和会话吊销完成验收。
4. 先以 Render 临时域名灰度验证，再决定是否绑定正式域名；旧站点保留一段观察期。
5. 发生错误时停止写入新站点，保留新库现场，回切旧站点；不要删除旧 Cloudflare 资产，直到新站点验收完成。

## 20-05 当前阻塞项

- Supabase 项目和空库结构已完成；Render 尚未连接代码仓库，也尚未配置 `DATABASE_URL`，因此还没有执行 Node 远端部署或生产切换。
- 生产 D1 导出和历史数据导入按用户决定跳过。
- Node 主机暂定 Render；项目已加入 `render.yaml`，正式域名尚未指定，首次上线可先使用 Render 临时域名。

这不是代码故障，而是需要数据库负责人/部署负责人提供外部权限后才能继续的上线步骤。
