# 06 AI 理解与方案

> 来源：`VibeCoding开发全流程SOP_详细可填写版.xlsx` 的 `06_AI理解`。
> 本阶段只做上下文、影响分析和方案，不直接修改业务代码。

## 06-01 已确认上下文

- 产品目标：对外开放的多租户团队项目进度平台。
- 访问方式：团队母账号注册；团队成员使用用户名和密码进入团队；不支持未加入团队的外部阅读。
- 隔离边界：公司/团队之间的数据、成员、项目、状态和资料互不影响。
- 数据：当前是硬编码演示数据；第一版做数据驱动，真实资料上传放到后续阶段。
- AI 边界：任何项目资料都不发送给 OpenAI 或其他外部 AI；第一版查询使用本地规则和数据库查询。
- 优先级：安全、项目架构、速度。
- 当前技术：React 19 + TypeScript + Vinext + Cloudflare Worker；Drizzle 已接入 D1，逻辑 binding 为 `DB`，本地和 Sites 生产环境均按该绑定运行。

## 06-02 影响分析

| 影响区域 | 需要调整的文件/模块 | 主要风险 |
| --- | --- | --- |
| 认证 | 新增认证服务、登录/注册页面、会话处理 | 密码存储、会话固定、暴力破解、登出失效 |
| 多租户 | 数据库表、所有 API 查询、服务端权限守卫 | 跨团队数据泄露；不能只在前端过滤 |
| 数据层 | `db/schema.ts`、D1 配置、迁移 | 迁移错误、索引缺失、状态引用删除 |
| 项目管理 | 新增项目 API/页面，替换 `project-data.ts` | 破坏现有演示页面、并发覆盖 |
| 自定义状态 | 状态配置 API/页面、项目状态关联 | 删除被引用状态、跨团队复用状态 |
| 查询 | `app/api/ask/route.ts`、`project-query.ts` | 继续调用 OpenAI、越权查询、无资料时编造 |
| 配置 | `.env.example`、`vite.config.ts`、hosting 配置 | 密钥泄露、D1 绑定未配置 |
| 测试 | API、数据隔离、认证和渲染测试 | 只验证页面，不验证安全边界 |
| 旧入口 | 根目录 `index.html` | 开发预览展示旧版，误判当前功能 |

## 06-03 方案比较

### 方案 A：D1 + 自建用户名/密码会话（推荐）

- 使用 Cloudflare D1 保存团队、成员、项目、状态和审计数据。
- 使用服务端密码哈希和 HttpOnly 会话 Cookie。
- 每个业务查询由服务端会话注入 `team_id`。
- 查询使用 SQL + 本地关键词规则，不依赖外部 AI。

优点：符合“资料不出受控环境”，租户边界清晰，部署形态与现有 Worker 一致，后续上传可接 R2。

风险：认证、密码策略、会话吊销和限流需要自行实现并重点测试。

### 方案 B：外部认证服务 + D1 业务数据

- 使用第三方身份服务处理用户名、密码、会话和找回密码。
- 本项目只保存团队和业务数据。

优点：减少自建认证代码。

风险：增加外部依赖和敏感账号数据流向；需要额外确认服务商、地区、合规和可用性，不符合当前“安全优先、架构可控”的首选方向。

## 推荐技术决策

采用方案 A：D1 + 服务端会话 + 本地查询。

1. v1 用 `organizations`/`teams` 维护公司与团队关系，所有业务表使用 `team_id` 作为强制范围。
2. 认证 API 与业务 API 分离，并提供统一的 `requireUser`/`requireTeamMember` 守卫。
3. 密码只保存随机盐和哈希结果；不在日志、响应或审计中保存明文。
4. 会话使用 HttpOnly、Secure、SameSite Cookie，并支持主动吊销。
5. `app/api/ask/route.ts` 移除 OpenAI 请求，只调用本地 `resolveProjectQuestion` 和数据库查询服务。
6. 项目资料上传不进入首个垂直切片；未来接入时使用受控对象存储并单独通过安全门禁。
7. 先用结构化 SQL/关键词查询实现速度和可解释性，暂不引入本地向量模型。

## 需要在实现前锁定的安全检查

- 跨团队访问测试必须覆盖列表、详情、更新、归档和查询接口。
- 认证失败必须有限流/锁定策略，不能返回可枚举的团队信息。
- 生产密钥和 D1/R2 绑定必须由部署环境注入，不能写入仓库。
- OpenAI 依赖和环境变量在进入真实数据前必须移除。
