# 当前版本不调用 OpenAI 或其他外部 AI 服务。
# Node/Vinext 运行时使用 Supabase PostgreSQL；请只在本地 .env 中填写连接串，
# 不要提交真实密码、服务密钥或连接串。
# DATABASE_URL=postgresql://postgres:<password>@<pooler-host>:6543/postgres?sslmode=require
#
# 迁移窗口内，Cloudflare 兼容预览仍可通过 .openai/hosting.json 的 DB binding
# 继续使用旧 D1 数据库，直到 PostgreSQL 线上验收完成。
