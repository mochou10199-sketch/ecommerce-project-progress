# 04 模块与数据

> 来源：`VibeCoding开发全流程SOP_详细可填写版.xlsx` 的 `04_模块与数据`。
> 设计原则：先确保多租户隔离和权限正确，再扩展资料上传与更多查询能力。

## 模块清单

| 模块 | 职责 | 不负责的内容 | 依赖 |
| --- | --- | --- | --- |
| 认证与会话 | 团队母账号注册、用户名/密码登录、退出、会话失效 | 不接入 OpenAI，不保存明文密码 | 用户、团队 |
| 公司/团队 | 创建公司租户、开启团队模式、维护团队标识 | 不读取其他公司数据 | 认证 |
| 成员与权限 | 创建/停用成员、角色判断、团队访问控制 | 不允许外部只读 | 公司/团队、认证 |
| 项目管理 | 项目新增、编辑、查看、归档、负责人和进度维护 | 不执行支付/订单/库存动作 | 成员、项目状态 |
| 项目状态配置 | 公司级状态新增、排序、颜色、停用 | 不跨公司共享状态 | 公司/团队 |
| 本地查询 | 在当前公司范围内按关键词/结构化条件查询 | 不调用外部 AI，不跨租户搜索 | 项目、状态、来源 |
| 来源记录 | 维护资料名称、来源说明和可追溯信息 | 第一版不做真实文件上传解析 | 项目 |
| 审计与安全 | 记录登录、权限拒绝、成员变更、项目关键修改 | 不记录密码和敏感原文 | 所有写操作 |

## 数据对象

### Organization / Company（公司租户）

- `id`
- `name`
- `slug`：用于登录或团队识别，唯一
- `status`：active / suspended
- `created_at`
- `updated_at`

### Team（团队模式）

- `id`
- `organization_id`
- `name`
- `enabled`
- `created_at`

第一版按“一个公司一个主团队”设计，但保留 `organization_id` 与 `team_id`，便于后续扩展多团队。

### User / TeamMember（成员账号）

- `id`
- `team_id`
- `username`
- `password_hash`
- `role`：当前团队角色 code；系统角色为 owner / member，自定义角色保存在 TeamRole
- `status`：active / suspended
- `must_change_password`
- `last_login_at`
- `created_at`
- `updated_at`

### TeamRole（团队权限角色）

- `id`
- `team_id`
- `code`
- `name`
- `description`
- `permissions`：项目查看、创建、编辑、归档；`team.manage` 只属于母账号系统角色
- `is_system`
- `is_active`
- `created_at`
- `updated_at`

约束：角色 code 只在当前团队内唯一；系统角色不可编辑；停用角色前必须先为成员分配其他角色；角色变更会吊销成员已有会话。

约束：同一团队内用户名唯一；新密码长度为 6-20 个字符且只存哈希。登录请求必须包含团队标识、用户名和密码，不能仅依赖前端传入的公司 ID。

### Session（登录会话）

- `id`
- `user_id`
- `team_id`
- `expires_at`
- `revoked_at`
- `created_at`

每次服务端读取业务数据时，都从有效会话得到 `team_id`，并在查询中强制带上团队范围。

### ProjectStatus（公司自定义项目状态）

- `id`
- `team_id`
- `name`
- `color`
- `sort_order`
- `is_active`
- `created_at`
- `updated_at`

约束：同一团队内名称唯一；被项目引用时只能停用或迁移，不能直接删除。

### Project（项目）

- `id`
- `team_id`
- `name`
- `stage`
- `status_id`
- `planned_end_date`
- `progress`
- `owner_user_id`
- `archived_at`
- `created_at`
- `updated_at`

项目状态和阶段分开保存。所有读取、更新、归档操作必须同时校验 `team_id`。

### ProjectMember（项目授权，可选但预留）

- `project_id`
- `user_id`
- `permission`：view / edit

第一版可以默认团队成员查看团队项目，后续敏感项目再启用项目级授权；数据模型预留该关系以避免重构。

### ProjectSource（来源记录）

- `id`
- `project_id`
- `team_id`
- `title`
- `source_type`：manual / upload（后续） / link（后续）
- `reference`
- `created_by`
- `created_at`

第一版只保存手工来源说明；上传文件和文档解析进入后续阶段，并且必须重新做安全评估。

### AuditLog（审计记录）

- `id`
- `team_id`
- `user_id`
- `action`
- `resource_type`
- `resource_id`
- `result`
- `created_at`

不得在审计日志中写入密码、会话令牌或未经脱敏的敏感资料原文。

## 数据关系

```text
Organization 1 ── 1..n Team
Team 1 ── n User
Team 1 ── n ProjectStatus
Team 1 ── n Project
Project 1 ── n ProjectSource
Project n ── n User（通过 ProjectMember，可选）
User 1 ── n Session
Team 1 ── n AuditLog
```

## 数据流

### 注册

团队母账号提交注册信息 → 服务端校验 → 创建 Organization/Team/User/Session → 初始化默认状态 → 返回当前团队首页。

### 项目维护

团队成员提交项目 → 服务端从 Session 获取 team_id → 校验角色和字段 → 保存 Project → 写入 AuditLog → 返回项目详情。

### 项目查询

用户提交查询 → 服务端读取 team_id → 在 `Project`、`ProjectStatus`、`ProjectSource` 的团队范围内查询 → 返回可验证字段和来源 → 无匹配时返回资料不足。

## 关键数据约束

1. 所有业务数据必须有 `team_id`；没有团队范围的数据不得进入业务表。
2. 所有跨表查询必须使用团队范围条件，并为关键索引准备 `team_id` 前缀。
3. 业务删除优先使用归档或停用，保留审计记录。
4. 外部资料上传后不得直接进入查询索引，必须先经过文件类型、大小、病毒/内容安全和权限检查。
5. 真实敏感字段的加密、脱敏和保留周期在上传阶段单独确认。
