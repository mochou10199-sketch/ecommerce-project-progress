# 14 上线验证

> 来源：`VibeCoding开发全流程SOP_详细可填写版.xlsx` 的 `14_上线验证`。

## 已完成验证

| 验证项 | 结果 | 说明 |
| --- | --- | --- |
| 线上入口 | 通过 | HTTPS 返回 200，页面包含团队入口应用 |
| 未登录门禁 | 通过本地与入口验证 | 未登录项目 API 返回 401；线上入口先进入会话确认页 |
| 团队隔离 | 通过本地回归 | 两个测试团队的数据互不可见 |
| 本地隔离接口验收 | 通过 | 临时 D1 实测注册两家团队、项目隔离、跨团队查询不可见和普通成员权限边界 |
| 普通成员权限 | 通过本地回归 | 成员调用成员/状态管理接口返回 403 |
| 停用会话失效 | 通过本地回归 | 成员停用后已有会话访问项目接口返回 401 |
| 成员密码重置 | 通过本地回归 | 母账号重置成员密码后旧密码返回 401，成员旧会话失效，新密码可登录并要求改密 |
| 外部 AI 禁用 | 通过 | 查询接口固定使用本地规则模式 |
| 健康检查 | 通过 | `/api/health` 线上返回 200，不返回团队或项目数据 |
| 应用层限流 | 通过本地回归 | 登录接口超过阈值返回 429 和 `Retry-After`；其他热点接口复用同一限流模块 |
| 团队自定义角色 | 通过本地隔离验收 | 母账号创建角色并分配成员；只读角色可读取项目但编辑返回 403；角色变更吊销旧会话 |
| 生产预检 | 通过 | `npm run production:preflight` 验证首页 200、健康检查 200、安全响应头、413 请求体拦截和 403 跨站写请求 |
| 项目近实时同步 | 通过 | 页面可见时每 15 秒同步项目列表；编辑项目时暂停自动覆盖，并支持立即刷新 |
| 项目并发更新 | 通过本地回归 | 项目保存携带服务端返回的更新时间版本；版本不一致返回 409，提示刷新后再编辑 |
| 线上真实团队低风险验收 | 通过 | 使用团队 001 母账号完成登录、会话身份、项目列表、项目状态、本地查询、运行监控、事件列表、退出和会话吊销；未创建或修改业务数据 |

## 尚未完成

- 当前线上 D1 尚未应用 `0005_team_roles.sql`；自定义角色已完成本地开发、隔离验收并保存为 Sites 候选版本，待具备数据库迁移权限后再部署到线上验证。
- 线上真实团队注册和创建项目尚未执行，避免在当前生产团队写入验收数据；需要维护窗口或专用验收团队后再执行。
- 线上 D1 备份、恢复、迁移回滚、边缘层全局限流和告警尚未演练；当前环境缺少 Cloudflare API/Zone 权限。
- 真实资料上传和敏感内容权限测试尚未开始。
