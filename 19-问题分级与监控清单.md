# 19 问题分级与监控清单

> 对应 SOP `15_维护运营` 的 15-01、15-02。以下 SLA 是上线初期的建议默认值，正式投入真实业务前需要由团队负责人确认。

## 19-01 问题分级

| 等级 | 判定标准 | 首次响应 | 处理要求 | 典型例子 |
| --- | --- | --- | --- | --- |
| P0 | 平台不可用、跨团队数据泄露、密码/会话安全事件、生产数据疑似损坏 | 15 分钟内 | 立即暂停相关写入，通知负责人，保留日志并启动回滚/恢复评估 | 团队 A 能看到团队 B 的项目 |
| P1 | 核心登录、项目保存或查询对多数用户不可用 | 1 小时内 | 优先恢复核心路径，记录影响范围和临时方案 | 所有团队无法保存项目 |
| P2 | 单一团队或部分功能受影响，有可行替代操作 | 1 个工作日内 | 排入当前迭代，补充回归测试和关闭证据 | 某个项目状态选项无法编辑 |
| P3 | 文案、样式、易用性或非阻断优化 | 2 个工作日内确认 | 纳入后续版本，不打断 P0-P2 处理 | 提示文案或布局优化 |

## 19-02 必须监控的指标

| 指标 | 当前检查方式 | 告警建议 | 注意事项 |
| --- | --- | --- | --- |
| `/api/health` 5xx | `npm run production:preflight`、Worker 日志 | 连续失败或 5 分钟内多次失败 | 响应内容只保留健康状态，不返回业务数据 |
| API 429 比例 | Worker 日志、Cloudflare 边缘规则 | 短时间异常升高 | 不记录密码、会话令牌和项目原文 |
| 登录失败/锁定 | 团队管理运行监控、`audit_logs` 和 Worker 日志 | 同一 IP/账号集中出现 | 只记录事件和结果，不记录密码 |
| D1 错误 | `/api/health`、Worker 日志 | 任意生产 D1 错误需调查 | 恢复前先备份并走审批 |
| 部署状态 | Sites 部署状态 | 构建或发布失败立即通知 | 只部署已保存、已验证版本 |
| 413/403 防护响应 | Worker 日志 | 短时间异常升高时调查 | 可能是攻击，也可能是误配置，不直接放宽限制 |

## 19-03 事件记录模板

```text
事件编号：INC-YYYYMMDD-001
发现时间：
等级：P0 / P1 / P2 / P3
发现渠道：健康检查 / Worker 日志 / 用户反馈 / 部署平台
影响团队：
影响功能：
现象与原始证据：
负责人：
临时措施：
是否暂停写入：是 / 否
是否需要回滚或恢复：是 / 否
修复版本：
回归结果：
关闭时间与关闭证据：
```

## 当前未完成项

- 应用已经具备健康检查、Worker 日志、应用层限流、安全响应头、生产预检，以及母账号可见的本地运行监控统计。
- 团队母账号已经可以按 P0-P3 创建、更新和关闭事件；事件记录按团队隔离并写入 `audit_logs`，不会替代 Cloudflare 告警渠道。
- 事件列表会根据建议响应窗口计算“待响应、已超时、已接手、已关闭”和响应节点；该计算是运营提醒，不是未经确认的正式 SLA。
- Cloudflare 边缘告警渠道和全局限流规则尚未配置，需要具备 Zone/账户权限的管理员执行。
- D1 备份恢复仍需确认远程数据库名称、备份位置、维护窗口和审批人。
