# 00 适配判断

> 来源：`VibeCoding开发全流程SOP_详细可填写版.xlsx` 的 `00_适配判断`。
> 本文件记录基于当前代码、演示数据和已确认边界的判断；接入真实业务资料后仍需按数据类型复核风险。

## 00-01 项目类型与风险分级

| 项目项 | 当前判断 | 证据/说明 |
| --- | --- | --- |
| 项目类型 | 对外开放的多租户电商项目进度平台、Web 看板、服务端本地事实查询 | `README.md`、`app/components/ProjectDashboard.tsx` |
| 当前用户 | 对外开放注册，但只有团队成员可以进入；每家公司拥有独立租户空间，并可在公司内开启团队模式 | 用户已确认不支持外部只读；当前已实现登录、租户和团队角色权限系统 |
| 当前数据 | D1 中按公司/团队隔离的项目、成员、状态、来源、审计和运维事件；真实资料上传尚未开放 | `db/schema.ts`、`README.md` |
| 当前风险等级 | 高风险建设项目 | 对外开放、多租户、真实敏感资料和登录权限同时存在；租户隔离失败可能导致公司间数据泄露 |
| 当前演示版本风险 | 中风险 | 当前公开版本已接入 D1 和认证；真实资料上传未开放，生产备份、边缘限流和正式演练仍待完成 |
| 真实资料接入要求 | 后续支持上传真实资料，但禁止发送给 OpenAI | 需要受控存储、权限控制、租户隔离、脱敏、审计和上传安全检查；不得依赖外部大模型处理资料 |
| 高风险领域 | 当前未发现医疗、金融交易、政务、支付执行或生产控制功能 | 以当前代码范围判断；新增相关功能时必须重新分级 |

## 00-02 AI 使用边界（已确认）

### 允许

- 阅读本项目源码、配置、测试结果和已批准的项目资料。
- 修改与当前任务直接相关的本地代码和文档。
- 执行本地安装、开发、测试、构建和静态检查命令。
- 依据已验证的项目数据生成摘要、查询结果和测试用例。

### 必须人工确认

- 接入真实项目资料、个人信息、商业机密或生产数据。
- 修改数据库结构、数据迁移、权限策略或认证逻辑。
- 向任何外部 AI 服务发送项目资料；当前本地查询路径已禁用外部 AI，后续变更若引入外部服务仍需重新人工确认。
- 部署、上线、生产环境操作、域名/权限变更和不可逆数据操作。

### 禁止

- 读取、输出、提交或上传真实 API 密钥、密码、令牌和生产凭据。
- 在没有资料依据时补全或推测项目状态。
- 将多个项目的资料混合成一个结论。
- 把演示数据或未经脱敏的内部资料直接当作公开数据使用。

## 00-03 安全底线（已确认初稿）

- 密钥只放在服务端环境变量或部署平台密钥配置中，不使用 `NEXT_PUBLIC_` 前缀。
- 当前 API 已限制问题长度，查询完全使用当前团队本地数据；后续若开放资料上传，必须继续保持资料不出受控环境。
- 真实项目资料接入前，补充数据分类、脱敏规则、访问控制、来源链接和审计记录，且资料不得离开受控环境。
- 每一条公司、团队、项目、资料和查询记录都必须带有租户范围；服务端必须强制校验，不能只依赖前端隐藏。
- 登录、邀请、团队成员、角色权限、跨租户访问、上传文件类型/大小和敏感日志都必须有测试证据。
- 真实数据接入前，补充错误日志脱敏、数据库备份、恢复方案和生产回滚门禁。
- “已完成”必须同时有实际结果和证据，不能只凭页面能打开判定完成。

## 已确认边界

- 用户不限定为内部电商团队。
- 系统需要接入真实项目资料。
- 不允许向 OpenAI 发送任何项目资料。
- 目标是把当前演示原型升级为数据驱动系统。
- 平台面向外部开放；每家公司是独立租户，公司内部可以开启团队模式。
- 不支持未加入团队的外部阅读，数据访问以团队成员身份为前提。
- 支持注册团队母账号，团队成员使用用户名和密码进入团队。
- 每家公司可以自定义自己的项目状态。
- 后续需要支持资料上传，且资料可能包含敏感信息。
- 第一版非功能优先级为安全、项目架构和速度。

## 仍需在后续变更阶段确认

1. 是否增加多个团队管理员，以及多个管理员之间的职责边界。
2. 后续上传的文件类型、大小、保留周期和可见范围。
3. 项目级授权是否需要在团队角色之外单独启用。
4. 后续上传的文件类型、大小、保留周期和可见范围。
