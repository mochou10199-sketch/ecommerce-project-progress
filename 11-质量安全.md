# 11 质量安全

> 来源：`VibeCoding开发全流程SOP_详细可填写版.xlsx` 的 `11_质量安全`。

## 11-01 安全检查结果

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 外部 AI 外发 | 通过 | `/api/ask` 已改为团队范围内的本地规则查询 |
| 密码明文保存 | 通过 | 使用随机盐和 PBKDF2 哈希，数据库不保存原密码 |
| 会话 Cookie | 通过 | HttpOnly、SameSite=Lax；HTTPS 下追加 Secure；支持退出吊销；成员停用会吊销现有会话；修改密码会吊销其他旧会话并保留当前会话 |
| 未登录访问 | 通过 | 项目接口和查询接口返回 401 |
| 团队隔离 | 通过首条切片 | 列表和查询均由服务端会话注入 `team_id` |
| 密码暴力破解 | 已补充基础门禁 | 连续 5 次错误后临时锁定 15 分钟，并记录审计事件 |
| 应用层限流 | 已补充基础门禁 | 登录、注册、改密、项目查询和项目创建按客户端/账号限流；429 响应带 `Retry-After` |
| 输入长度与格式 | 通过 | 注册和成员创建的新密码统一限制为 6-20 个字符；其他接口继续执行字段长度/格式限制 |
| 文件上传安全 | 未开始 | 上传属于后续阶段，当前没有文件入口 |
| 成员停用/权限变更 | 通过 | 团队母账号可创建、停用、恢复、重置成员密码和分配团队自定义角色；停用、重置密码或角色变更会吊销成员现有会话；母账号权限受保护；角色权限由服务端按当前团队角色解析；普通成员不能调用管理接口；新成员首次登录必须修改初始密码 |
| 生产密钥/部署绑定 | 基础通过 | 当前公开站点已部署；边缘层全局限流、D1 备份恢复和正式演练仍待完成 |
| 运营快照导出 | 通过基础门禁 | 仅团队母账号可导出当前团队数据；不包含密码、密码哈希、会话令牌；导出操作写入审计记录；不能替代生产 D1 备份 |
| 响应安全头 | 通过 | Worker 统一增加 nosniff、DENY、Referrer-Policy、Permissions-Policy、COOP；HTTPS 下启用 HSTS |
| API 请求体边界 | 基础通过 | Worker 对带 Content-Length 的 POST/PUT/PATCH 请求限制为 256 KiB，超限返回 413；无 Content-Length 的流式请求仍需由边缘规则兜底 |
| API 同源写入 | 通过 | Worker 对带 Origin 的 API 写请求校验来源，仅允许当前站点同源提交；跨站或格式异常来源返回 403 |

## 11-02 质量检查结果

- 业务数据查询、项目创建、认证和租户范围已分离为独立模块。
- 旧静态入口已移至 `docs/legacy/`，当前运行入口统一为 `app/`。
- D1 schema 已生成迁移，关键团队字段有索引。
- 项目编辑/归档 API/UI 已实现并完成本地回归；项目编辑使用 `updatedAt` 乐观并发校验，过期版本返回 409；成员管理、团队自定义角色和状态配置 API/UI 已实现，角色变更会吊销旧会话；健康检查、应用层限流和团队运营快照导出已接入；生产迁移回滚测试仍待补。

## 11-03 数据正确性检查

- 项目状态和项目阶段使用不同字段。
- 项目来源单独存储，查询结果可返回来源标题。
- 项目归属以服务端会话中的 `team_id` 为准，不信任客户端租户参数。
- 状态被项目使用时禁止直接删除，管理 API/UI 已实现为归档并返回冲突提示。
- 当前没有金额计算、库存扣减、订单状态迁移等高风险数据动作。

## 阻断项

在进入生产或真实敏感资料前，必须完成：

1. Cloudflare 边缘层全局限流、D1 备份恢复和生产演练。
2. 项目编辑/归档、D1 迁移、备份、恢复和回滚演练。
3. 上传功能的文件类型、大小、内容安全、权限和保留周期设计。
4. 生产环境密钥、域名、限流和监控配置。

本轮补充的响应头和请求体边界属于应用层防线，不能替代 Cloudflare 边缘层限流、WAF 规则或上传功能的专门内容安全检查。
