# 13 部署上线

> 来源：`VibeCoding开发全流程SOP_详细可填写版.xlsx` 的 `13_部署上线`。

## 发布结果

- 已创建 Sites 项目并保存可回滚版本。
- 当前版本已部署成功，入口为：`https://ecommerce-project-progress.mochou10199.chatgpt.site`。
- 最新线上版本已包含生产预检、响应安全头、API 请求体边界、同源写入校验、项目近实时同步、P0-P3 事件闭环和线上低风险验收脚本。
- 站点入口设置为公开访问，应用数据仍由团队编号、用户名和密码保护。
- 已初始化团队编号 `001` 的平台管理团队母账号；账号密码不写入代码、文档或日志。
- 未配置外部 AI 密钥；R2 未启用，真实资料上传尚未开放。
- 团队自定义角色已完成本地验收并保存为 Sites 候选版本，但线上 D1 尚未应用 `0005_team_roles.sql`，因此尚未部署、未纳入当前生产版本。

## 发布边界

- 未导入真实公司项目资料和真实成员密码。
- 当前生产环境已增加 `/api/health` 健康检查和应用层限流；线上真实团队低风险验收已完成；真实 D1 备份恢复、边缘层限流和完整生产演练记录见 `17-生产演练.md`，尚未完成。
- 若发现问题，回退到上一条已保存版本，不直接修改线上数据库。
