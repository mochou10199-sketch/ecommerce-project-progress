# 16 版本归档

> 来源：`VibeCoding开发全流程SOP_详细可填写版.xlsx` 的 `16_版本归档`。

## 归档信息

- 开发分支：`codex/sop-foundation`
- 当前主线提交：`67207dc`，包含隔离团队基础、认证安全、首次密码修改、成员/状态管理、团队自定义角色、应用层限流、健康检查、P0-P3 事件和生产演练门禁。
- 数据库迁移顺序：`drizzle/0000` → `drizzle/0001` → `drizzle/0002` → `drizzle/0003` → `drizzle/0004` → `drizzle/0005`；`0004` 增加幂等的 P0-P3 事件表和索引，`0005` 增加每团队独立的权限角色表。
- 线上版本已保存并成功部署；旧版本仍可用于回滚。
- 右侧浏览器已切换到线上入口；本地 `npm run dev` 仍可继续开发。

## 下一版未发布变更

- 团队自定义角色和成员角色分配已完成隔离本地验收，功能提交为 `5efc34a`，生产门禁提交为 `412e46a`，操作手册提交为 `67207dc`，并已保存为 Sites 候选版本。
- 线上 D1 只读检查尚未发现 `team_roles` 表，因此候选版本暂不部署；待数据库负责人应用 `0005` 并完成生产迁移验证后再部署并执行线上权限验收。

## 归档边界

本版本不包含真实资料、外部 AI 调用、外部只读分享和文件上传。项目编辑/归档、结构化项目创建、健康检查、应用层限流和迁移顺序门禁已纳入当前版本；D1 真实备份恢复、边缘层限流和生产演练仍需按 `docs/vibe-coding/17-生产演练.md` 执行。
