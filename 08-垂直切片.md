# 08 垂直切片

> 来源：`VibeCoding开发全流程SOP_详细可填写版.xlsx` 的 `08_垂直切片`。

## 首条最小路径

```text
注册团队母账号
  → 创建公司/团队空间
  → 初始化公司默认项目状态
  → 登录并进入团队
  → 创建项目
  → 项目列表/详情展示
  → 团队范围内查询项目
```

## 本切片验证的架构假设

1. D1 可以在当前 Cloudflare Worker/Vinext 架构中稳定提供持久化。
2. 用户名/密码和服务端会话可以在 Worker 环境中安全运行。
3. 所有业务表使用 `team_id` 可以形成可审计的租户隔离边界。
4. 当前 React 页面可以替换硬编码数据而不破坏现有视觉结构。
5. 本地规则查询足以覆盖第一版项目状态、阶段、负责人和风险查询。

## 切片验收步骤

1. 注册两个不同的团队母账号，分别创建团队 A 和团队 B。
2. 在团队 A 创建项目 A，在团队 B 创建项目 B。
3. 登录团队 A，确认只能看到项目 A；尝试访问项目 B 的标识，必须被拒绝。
4. 在团队 A 查询项目 A，结果只来自团队 A 的数据。
5. 登出后直接访问项目接口，必须返回未登录错误。
6. 输入不存在的项目名称，必须返回资料不足，不能调用外部 AI。
7. 运行 lint、构建和自动化测试。

## 不包含

- 真实文件上传和文档解析。
- 团队成员管理的完整后台。
- 复杂项目级权限。
- OpenAI 或其他外部 AI 调用。
- 生产部署和真实敏感数据。

