# 17 生产演练

> 本文对应 SOP 的上线验证、回收复盘和安全收口要求，记录当前公开站点的低风险演练步骤。

## 17-01 线上健康检查

健康检查只验证 Worker 和 D1 是否可用，不返回团队、成员、项目或审计数据：

```bash
curl -i https://ecommerce-project-progress.mochou10199.chatgpt.site/api/health
```

通过标准：HTTP 200、`ok` 为 `true`；失败时只返回 HTTP 503 和 `ok: false`，详细错误留在服务端日志。

## 17-01-02 线上真实团队低风险验收

不把验收账号写入仓库，使用环境变量运行只读业务闭环；脚本会登录、读取会话、项目列表、项目状态、当前团队本地查询、运行监控和事件列表，然后退出并验证会话已吊销：

```bash
SMOKE_TEAM_CODE="<团队编号>" \
SMOKE_USERNAME="<验收用户名>" \
SMOKE_PASSWORD="<一次性验收密码>" \
npm run production:smoke
```

通过标准：登录、会话身份、项目列表、项目状态、`local` 查询、母账号监控、事件列表均返回 200；退出后 `/api/auth/me` 返回 401。脚本不创建项目、成员或事件，不打印密码和会话 Cookie。

本轮验收结果：团队 001 母账号低风险闭环通过；登录、会话、项目列表、项目状态、本地查询、运行监控、事件列表、退出和会话吊销均符合预期，未写入项目、成员或事件业务数据。

## 17-01-03 迁移顺序门禁

发布前执行 `npm run db:migrations:check`。脚本在临时隔离的本地 D1 中按 `0000` → `0001` → `0002` → `0003` → `0004` → `0005` 执行迁移，重复执行末次幂等迁移，并校验核心表与登录锁定、项目扩展字段、团队角色、P0-P3 事件字段。脚本只操作临时本地状态，不连接远程 D1，也不执行恢复。

生产数据库由数据库负责人执行 `0005_team_roles.sql` 后，使用 `PROD_D1_NAME`、`CLOUDFLARE_ACCOUNT_ID` 和 `CLOUDFLARE_API_TOKEN` 运行 `npm run production:roles:check`。该命令只读验证 `team_roles` 及其索引，不代替迁移，也不打印凭证。

## 17-01-04 本地隔离接口验收

执行 `npm run local:acceptance`，脚本会启动独立的临时 Worker 和 D1，完成两团队注册、团队自定义只读角色、角色变更旧会话吊销、只读角色查看成功但编辑 403、母账号重置成员密码、旧密码拒绝、项目列表隔离、跨团队查询不可见、普通成员管理接口 403、修改密码后的其他旧会话吊销、连续错误密码后的账号锁定，以及多人编辑时旧版本返回 409。脚本结束后会停止 Worker 并清理临时数据库，不写入线上或现有本地 D1。

## 17-02 D1 备份

生产数据库名称以 Sites/D1 控制台为准，不写入客户端代码。备份前记录发布版本和数据库名称，并将导出文件放在受控目录；部署包含 `0005` 后，备份结构必须包含 `team_roles`：

```bash
PROD_D1_NAME="<生产 D1 数据库名>"
BACKUP_FILE="/受控备份目录/ecommerce-project-$(date -u +%Y%m%dT%H%M%SZ).sql"

./node_modules/.bin/wrangler d1 export "$PROD_D1_NAME" \
  --remote --skip-confirmation --output "$BACKUP_FILE"
```

备份验收：文件存在、可读、包含 `organizations`、`teams`、`users`、`team_roles`、`projects`、`audit_logs` 表结构；备份文件不得提交 Git 或上传给外部 AI。

### 17-02-01 生产备份前置检查

项目提供了受控脚本，只有同时提供准确的生产 D1 名称、Cloudflare 账户 ID、API Token 和项目目录外的备份目录时才会执行导出。脚本会校验当前全部核心表（包括 `team_roles` 和 `incidents`），拒绝覆盖已有文件，不执行恢复，也不会把凭证写入文件或日志：

```bash
PROD_D1_NAME="<控制台确认的生产 D1 名称>" \
CLOUDFLARE_ACCOUNT_ID="<Cloudflare 账户 ID>" \
CLOUDFLARE_API_TOKEN="<具备 D1 读取权限的 Token>" \
BACKUP_DIR="/受控备份目录" \
npm run production:backup:check
```

当前环境没有 Cloudflare API 权限，因此只完成脚本和门禁实现，未执行真实生产导出。

## 17-03 恢复演练

恢复是高风险操作，必须在维护窗口、明确审批和已有新备份后执行。优先使用 D1 Time Travel 获取时间点信息：

```bash
./node_modules/.bin/wrangler d1 time-travel info "$PROD_D1_NAME" \
  --remote --timestamp "<RFC3339 时间点>" --json
```

确认时间点后，才允许由数据库负责人执行恢复：

```bash
./node_modules/.bin/wrangler d1 time-travel restore "$PROD_D1_NAME" \
  --timestamp "<RFC3339 时间点>"
```

恢复后必须依次检查：`/api/health`、团队登录、团队项目列表、查询结果、状态编辑、成员权限和跨团队不可见性。恢复演练不能直接在真实业务高峰期进行。

## 17-04 限流与监控

- 应用层已对登录、团队注册、首次改密码、项目查询和项目创建设置限流；触发时返回 429 和 `Retry-After`。
- Worker 已统一返回基础安全响应头，并对带 `Content-Length` 的 API 写请求设置 256 KiB 上限；超限返回 413。未带长度的流式请求仍应由边缘层规则限制。
- 应用层限流是单 Worker 实例的快速防线，不能替代 Cloudflare 边缘层的全局限流；生产域名仍需配置边缘规则和告警。
- 监控至少覆盖：健康检查 5xx、429 比例、登录失败/锁定审计事件、D1 错误和部署失败。
- 告警内容不得包含密码、会话令牌、项目原文或跨团队数据。

## 17-05 回滚规则

1. 先保留当前线上版本和 D1 备份。
2. 应用问题优先回滚到上一条已验证的 Sites 版本，不直接修改线上数据库。
3. 数据问题先暂停写入，再按恢复审批流程处理。
4. 回滚后重新执行健康检查、登录、权限和租户隔离验收。
