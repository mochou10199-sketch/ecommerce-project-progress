# 12 预发布检查

> 来源：`VibeCoding开发全流程SOP_详细可填写版.xlsx` 的 `12_预发布检查`。

## 检查结果

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 运行入口 | 通过 | 旧静态入口已归档，开发和构建均使用 `app/` |
| 构建与测试 | 通过 | `npm run lint`、`npm run build`、`npm test` 已通过 |
| D1 配置 | 已配置逻辑绑定 | `.openai/hosting.json` 使用 `DB`；`npm run db:migrations:check` 在隔离本地 D1 中按 `0000` → `0001` → `0002` → `0003` → `0004` → `0005` 执行并校验核心表/字段 |
| 团队角色生产迁移 | 待外部权限 | 线上只读数据库检查显示当前仍缺少 `team_roles`；发布角色功能前必须由数据库负责人执行 `0005_team_roles.sql`，再运行 `npm run production:roles:check` 验证表和索引；当前代码保留旧团队兼容回退 |
| R2 配置 | 暂不启用 | 上传属于后续阶段，当前保持 `null` |
| 外部 AI | 通过 | 查询接口只使用当前团队本地数据，未配置 OpenAI 密钥 |
| 生产密钥 | 已按规则处理 | 不把真实密钥写入仓库；生产环境由托管平台注入，当前未配置外部 AI 密钥 |
| 团队访问 | 已实现基础门禁 | 站点入口可访问，但项目数据必须通过团队编号、用户名和密码认证；母账号可管理成员和状态 |
| 健康检查与应用层限流 | 已实现基础门禁 | `/api/health` 不返回业务数据；登录、注册、改密、查询和项目创建具备 429 限流响应 |
| 团队运营快照 | 已实现基础门禁 | 仅母账号可下载当前团队快照；不包含密码和会话令牌；不能替代生产 D1 备份 |
| 团队运行监控 | 已实现基础门禁 | 仅母账号可查看当前团队的活跃成员、活跃项目、登录失败和安全异常统计；不返回项目原文 |
| P0-P3 事件记录 | 已实现基础门禁 | 仅母账号可创建、更新和关闭当前团队事件；状态和等级使用服务端枚举校验，并写入审计记录 |
| 响应安全与写入门禁 | 已实现基础门禁 | Worker 统一增加安全响应头、256 KiB API 请求体边界和同源写请求校验 |
| 项目同步 | 已实现 | 页面可见时每 15 秒同步，编辑期间暂停，支持立即刷新 |
| 本地隔离验收 | 通过 | `npm run local:acceptance` 覆盖注册、双团队隔离、母账号重置成员密码、成员权限、密码修改后的旧会话吊销、登录锁定和并发编辑冲突；临时数据库结束后清理 |
| 迁移顺序门禁 | 通过本地隔离检查 | 末次幂等迁移重复执行通过；生产 D1 备份、恢复和回滚仍待 Cloudflare 权限与维护窗口 |

## 发布前阻断项

当前版本已部署公开入口，但正式导入真实敏感资料前仍需：

1. 配置 Cloudflare 边缘层全局限流、监控告警和备份策略。
2. 完成 D1 生产备份、恢复和迁移回滚演练，并确认 `0000` → `0001` → `0002` → `0003` → `0004` → `0005` 的顺序。
3. 完成线上真实团队注册、登录、创建项目和查询闭环演练。
4. 设计并实现后续上传的文件安全、权限和保留周期。

## 回滚说明

- 代码回滚：回退到上一条已验证提交，不覆盖用户目录中的其他仓库。
- 数据库回滚：先备份生产 D1，再按迁移版本执行恢复；禁止直接删除生产表。
- 站点回滚：只部署已保存且通过构建的版本，不把未验证工作区直接发布。
