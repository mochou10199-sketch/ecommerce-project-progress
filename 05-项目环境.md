# 05 项目环境

> 来源：`VibeCoding开发全流程SOP_详细可填写版.xlsx` 的 `05_项目环境`。

## 05-01 仓库与分支

- 已在项目目录建立独立 Git 仓库，避免使用覆盖用户目录的外层仓库。
- 默认主分支：`main`。
- 当前开发分支：`codex/sop-foundation`。
- 项目忽略规则已包含依赖、构建产物、环境变量、Wrangler 状态和调试日志。
- 已按垂直切片提交：`e204aa6`、`b9404bf`、`3e2cc82`。

## 05-02 运行基线

| 用途 | 命令 | 当前情况 |
| --- | --- | --- |
| 安装 | `npm ci` | 已执行成功 |
| 开发预览 | `npm run dev` | 可启动，根旧静态入口已归档，展示当前 React 页面 |
| 生产预览 | `npm run start` | 构建后可启动当前 React 页面 |
| 静态检查 | `npm run lint` | 已通过 |
| 测试 | `npm test` | 已通过，包含构建和 17 个测试 |
| 构建 | `npm run build` | 已通过 |
| 数据库迁移生成 | `npm run db:generate` | 已生成 `drizzle/0000` 至 `drizzle/0005` 业务迁移；`0005` 增加团队权限角色 |

## 05-03 环境与配置现状

- `.env.example` 已不再声明 OpenAI 或其他外部 AI 配置；本版本的查询只使用团队本地数据。
- `.openai/hosting.json` 已配置 D1 binding `DB`，R2 仍为 `null`，上传能力尚未开启。
- `db/schema.ts` 已包含公司、团队、成员、会话、项目、状态、来源、审计和运维事件表。
- 本地 Wrangler D1 已按 `0000` → `0001` → `0002` → `0003` → `0004` → `0005` 完成隔离迁移验证；生产环境的边缘限流、备份、恢复和告警仍需外部权限。

## 当前环境风险

1. 生产 D1 的真实备份、恢复和迁移回滚仍需具备 Cloudflare 权限的管理员执行。
2. 生产环境的边缘限流、告警、监控和域名规则尚未完成配置。
3. 上传、敏感内容处理和找回密码仍未实现；成员管理和状态管理已纳入当前版本。
