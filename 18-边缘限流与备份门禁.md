# 18 边缘限流与备份门禁

> 对应 SOP 的 `15_维护运营`、`14_上线发布` 和 `17-生产演练.md`。本阶段只记录可验证的生产准备，不绕过权限执行高风险操作。

## 当前检查结果

- Sites 生产数据库绑定：`DB`。
- 已确认用户表：`audit_logs`、`organizations`、`project_sources`、`project_statuses`、`projects`、`sessions`、`teams`、`users`。
- 应用层限流、响应安全头和 256 KiB API 请求体边界已部署。
- API 写请求已增加同源校验；跨站或格式异常 `Origin` 返回 403。
- 团队母账号可下载当前团队运营快照，包含项目、状态、成员元数据和最近 1000 条安全活动；不包含密码、密码哈希或会话令牌。
- 团队母账号可记录 P0-P3 运维事件并更新处理状态；该记录用于人工闭环，不等同于 Cloudflare 自动告警。
- 可运行 `npm run production:preflight` 验证首页、健康检查、安全响应头和 413 请求体拦截。
- 可运行 `npm run db:migrations:check` 在隔离本地 D1 中验证全部迁移顺序、末次幂等性和核心字段；该检查不连接远程数据库。
- 已提供 `npm run production:roles:check` 生产角色表只读检查；它要求显式 D1/Cloudflare 参数，只读取 `team_roles` 和两个索引，不执行迁移或写入。
- Cloudflare 全局限流规则尚未配置；当前环境没有 `CLOUDFLARE_API_TOKEN`，不能读取远程账户/区域规则或代替管理员写入边缘配置。
- D1 生产导出和恢复尚未执行；不能仅凭逻辑绑定名猜测远程数据库名称。
- 已提供 `npm run production:backup:check` 备份前置检查脚本：要求显式 Cloudflare/D1 参数和项目目录外的输出目录，导出后校验全部核心表，拒绝覆盖文件且不执行恢复。

## 18-01 Cloudflare 全局限流待配置项

由具备域名/Zone 权限的管理员在 Cloudflare 控制台或 API 中配置，并在变更记录中保存规则 ID、发布时间和回滚方式：

| 规则范围 | 建议维度 | 建议动作 |
| --- | --- | --- |
| `/api/auth/login`、`/api/auth/register`、`/api/auth/password` | IP + 路径 | 超限阻断并保留应用层 429 |
| `/api/ask` | IP + 路径 | 超限阻断或短暂挑战 |
| `/api/projects*`、`/api/team/*` | IP + 路径 | 超限阻断，避免写接口被刷 |
| 其余 `/api/*` | IP + 路径 | 设置较宽的全局兜底阈值 |

验收要求：规则只作用于当前站点 API，不影响静态页面；正常登录、项目查询和项目保存通过；超限响应可观察；规则可以按 ID 回滚。

## 18-02 D1 备份恢复门禁

在取得 Cloudflare API 权限、确认远程数据库名称并安排维护窗口后，按 `docs/vibe-coding/17-生产演练.md` 执行：

1. 记录当前线上版本、数据库名称和备份时间。
2. 先导出一份新备份，并在受控目录检查表结构和文件可读性。
3. 只在隔离环境或明确批准的维护窗口执行 Time Travel 恢复。
4. 恢复后依次验证健康检查、登录、项目列表、项目查询、状态管理、成员权限和团队隔离。
5. 保存演练记录；若失败，立即停止写入并按回滚审批处理。

禁止：把备份文件提交 Git、发送给外部 AI、在未备份时直接恢复、或使用猜测的数据库名称执行远程命令。

## 18-03 运营快照的边界

运营快照是团队母账号可主动下载的便携数据副本，用于人工留存和故障排查；它不是 D1 的完整生产备份，也不提供浏览器内恢复能力。正式备份仍需取得 Cloudflare 权限、确认远程数据库名称并按 `17-生产演练.md` 执行。

## 18-04 备份前置脚本边界

脚本只负责在权限和数据库名称明确后执行一次 D1 导出并校验文件结构；它不会猜测数据库名称、不会写入 Cloudflare 边缘规则、不会执行 Time Travel 恢复，也不会把备份文件放进项目目录。恢复仍需备份负责人、维护窗口和明确审批。
