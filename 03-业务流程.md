# 03 业务流程

> 来源：`VibeCoding开发全流程SOP_详细可填写版.xlsx` 的 `03_业务流程`。
> 当前目标是先把公司/团队边界、账号生命周期、项目生命周期和本地查询链路定清楚，再设计数据库和页面。

## 业务边界

- 公司是租户边界，所有公司项目、成员、状态和资料必须带有公司范围。
- 公司开启团队模式后，团队母账号管理本公司团队。
- 未加入团队的用户不能读取团队数据。
- 第一版不上传真实文件，不调用 OpenAI，不执行支付、订单或库存动作。

## 03-01 注册与进入团队流程

```text
未登录
  → 注册团队母账号
  → 创建公司/团队空间
  → 初始化公司项目状态
  → 自动进入团队首页
  → 创建成员账号
  → 成员使用用户名和密码登录
  → 服务端确认成员所属公司
  → 展示该公司项目总览
```

### 关键规则

1. 注册时创建唯一公司租户和团队母账号。
2. 公司名称、母账号用户名和密码必须通过服务端校验。
3. 密码只保存哈希值，不保存明文。
4. 成员登录后，服务端从会话获取公司范围，不接受前端传入的公司 ID 作为权限依据。
5. 停用成员后，旧会话必须失效。

## 03-02 团队成员管理流程

```text
团队母账号登录
  → 打开成员管理
  → 创建成员用户名和初始密码
  → 选择团队自定义成员权限角色
  → 成员首次登录并修改密码
  → 成员查看/编辑授权项目
  → 母账号可停用成员
```

当前角色：

| 角色 | 可做 | 不可做 |
| --- | --- | --- |
| 团队母账号 | 管理成员、项目、项目状态、团队设置 | 不得访问其他公司的数据 |
| 团队成员 | 按当前团队分配的角色查看、创建、编辑或归档项目、查询项目事实 | 不得管理成员、角色和项目状态 |

母账号可以为当前团队创建自定义项目权限角色；角色至少包含“查看项目”，可组合“创建项目”“编辑项目”“归档项目”。母账号权限不可转授。成员角色变更会吊销旧会话，防止旧权限继续使用。

## 03-03 项目生命周期

```text
创建项目
  → 填写项目基本信息
  → 选择公司自定义状态
  → 进入项目阶段
  → 更新进度/风险/阻塞项/负责人
  → 记录更新时间和来源
  → 继续更新或归档
```

### 项目状态与阶段

- 状态：由每家公司自定义，例如“正常”“风险”“延期”“已完成”。
- 阶段：描述项目正在做什么，例如“需求”“开发”“测试”“上线”。
- 状态和阶段分开存储，不允许用一个字段混合表达两者。
- 已被项目使用的状态只能停用或迁移，不能直接删除。
- 项目默认采用归档，不做不可恢复的直接删除。

## 03-04 项目查询流程

```text
团队成员提交问题
  → 服务端读取当前会话的公司范围
  → 本地规则解析项目关键词/查询意图
  → 只检索该公司的项目和来源记录
  → 命中单项目：返回事实、风险和来源
  → 命中多个项目：要求确认，不混合结论
  → 无命中：返回资料不足
```

查询结果不得调用外部 AI 生成或补全。查询必须返回可追溯的项目字段和来源记录。

## 03-05 关键异常流程

| 异常 | 处理 |
| --- | --- |
| 公司名称/用户名重复 | 拒绝创建，返回可理解提示，不泄露其他公司信息 |
| 密码错误过多 | 限流或临时锁定，并记录安全事件 |
| 访问其他公司项目 | 服务端按公司范围拒绝，不能通过错误信息推断资源存在 |
| 成员已停用 | 拒绝新请求并使旧会话失效 |
| 项目查询无资料 | 返回资料不足，不编造结论 |
| 状态被项目引用 | 禁止删除，先要求迁移或停用 |
| 保存失败 | 不产生半条项目记录，返回可重试错误 |

## 状态流转表

| 对象 | 状态 | 触发动作 | 操作者 | 失败处理 |
| --- | --- | --- | --- | --- |
| 团队 | 注册中 → 已启用 | 注册并完成校验 | 团队母账号 | 校验失败则不创建 |
| 成员 | 邀请/创建 → 可用 | 母账号创建并设置初始密码 | 团队母账号 | 用户名冲突则拒绝 |
| 成员 | 可用 → 已停用 | 停用成员 | 团队母账号 | 已停用成员不得访问 |
| 项目 | 活跃 → 已归档 | 归档项目 | 母账号/授权成员 | 无权限则拒绝 |
| 项目状态 | 启用 → 已停用 | 停用状态 | 团队母账号 | 被引用时不得删除 |
