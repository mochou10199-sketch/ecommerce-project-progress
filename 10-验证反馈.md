# 10 验证反馈

> 来源：`VibeCoding开发全流程SOP_详细可填写版.xlsx` 的 `10_验证反馈`。
> 本记录覆盖首条垂直切片，不代表生产上线验收。

## 10-01 自动化检查

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| `npm run lint` | 通过 | ESLint 无错误 |
| `npm run build` | 通过 | Vinext 五个构建环境均完成 |
| `npm test` | 通过 | 17 个测试通过 |
| `npm run db:migrations:check` | 通过 | 在隔离本地 D1 中按 `0000` → `0001` → `0002` → `0003` → `0004` → `0005` 顺序执行，并重新执行末次幂等迁移；核心表和关键字段校验通过 |
| `npm run local:acceptance` | 通过 | 临时 D1 中完成两团队注册、团队自定义只读角色、角色变更吊销旧会话、只读角色查看成功但编辑 403、母账号重置成员密码、旧密码拒绝、密码修改后的旧会话吊销、5 次错误密码锁定和旧版本编辑 409 验收；测试结束后清理临时状态 |
| 页面服务端渲染 | 通过 | 未登录展示团队会话入口，不渲染旧硬编码项目 |
| 外部 AI 路径扫描 | 通过 | `/api/ask` 只调用本地查询；测试确认无 OpenAI URL 和密钥配置 |

## 10-02 首条切片人工/接口场景

| 场景 | 结果 |
| --- | --- |
| 团队 A 注册母账号 | 通过，返回团队编号并写入 D1 |
| 团队 B 注册母账号 | 通过，返回独立团队编号 |
| 团队 A 创建项目 A | 通过，项目持久化并返回项目 ID |
| 团队 B 创建项目 B | 通过，项目持久化并返回项目 ID |
| 团队 A 查询项目列表 | 通过，只返回项目 A，不返回项目 B |
| 未登录访问 `/api/projects` | 通过，返回 401 |
| 团队 A 查询项目 A | 通过，返回项目状态、阶段、进度、阻塞项和来源 |
| 查询不存在的项目 | 通过，返回资料不足类结果 |
| 并发编辑保护 | 通过，项目更新携带 `updatedAt` 版本；版本过期返回 409，不静默覆盖较新的项目内容 |
| 浏览器未登录入口 | 通过，展示团队编号、用户名和密码登录页面 |
| 团队自定义权限角色 | 通过，母账号可创建角色并为成员分配；只读角色可查看但不能编辑，角色变更会吊销旧会话 |

## 当前仍未完成项

- 真实资料上传、敏感字段分级和文件安全检查仍未开始，按需求暂不开放。
- 线上 D1 尚未应用 `0005_team_roles.sql`；团队自定义角色已完成本地验收并保存为 Sites v34，待迁移权限后执行线上验收。
- D1 迁移链的隔离本地顺序校验已自动化，但生产 D1 备份、恢复和迁移回滚仍需 Cloudflare 权限、维护窗口和明确审批。
- Cloudflare 边缘层全局限流、外部告警渠道和正式生产演练仍需具备 Zone/账户权限的管理员执行。

以上边界必须在导入真实敏感资料或扩大生产使用范围前完成相应审批和演练。
